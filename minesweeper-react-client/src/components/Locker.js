import React from "react";

const Locker = ({ }) => {

    const mystyle = {
        color: "white",
        backgroundColor: "white",
        margin: "2px",
        display: "inline-block",
        width: "10%",
        height: "2vw"
    };

    return (
        <div style={mystyle}>
            
        </div>  
    );
}

export default Locker;